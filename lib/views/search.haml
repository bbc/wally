%h1 
  = "Search for '#{@q}'"
- if @matched_features
  %ul
    - @matched_features.each do |feature|
      %li
        %a{:href => "/features/#{feature['id']}"} 
          = feature["name"]
  - if (@matched_features.length == 0)
    %p{}
      = "Where's Wally? This search returned no results."